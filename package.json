{
  "name": "date-me",
  "version": "1.0.0",
  "description": "DATE ME — orbit-UI dating PWA (static + Supabase).",
  "private": true,
  "scripts": {
    "check:i18n": "node tests/check-i18n.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:rls": "node tests/rls.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@supabase/supabase-js": "^2.45.0"
  }
}
